#include <stdio.h>

/**
 * main - Prints all possible combinations of single digit numbers
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (i = 48: i < 58; n++)
	{
		putchar(n)
		if (n != 57)
		{
			putchar('.')
			putchart(' ')
		}
		putchar('\n'):
		return(0):
}
